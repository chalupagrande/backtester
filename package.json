{
  "name": "backtester",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Jamie Skinner <jamie@etcetera.cr>",
  "license": "MIT",
  "scripts": {
    "test": "yarn --cwd packages/core test",
    "testAll": "lerna run test",
    "dev": "yarn --cwd packages/server dev",
    "start": "yarn --cwd packages/server start"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
